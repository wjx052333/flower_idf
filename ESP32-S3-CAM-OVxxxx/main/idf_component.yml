dependencies:
  # Camera video framework (includes esp_cam_sensor and sensor drivers)
  espressif/esp_video: '>=1.4.1'
  # Software JPEG encoder for non-native-JPEG camera output
  espressif/esp_new_jpeg: '>=1.0.0'
  idf: '>=5.5'
  espressif/esp_audio_codec: ^2.4.0
